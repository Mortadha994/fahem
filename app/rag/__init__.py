"""The curriculum: chunking, retrieval, the pinned syntax context, the stored
chapters. This is what makes an answer grounded in the student's own textbook
rather than in whatever the model happens to know."""
